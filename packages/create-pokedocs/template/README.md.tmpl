# {{SITE_NAME}}

Documentation site built with [PokeDocs](https://wbaxterh.github.io/pokedocs/).

```bash
npm install
npx playwright install chromium   # once — renders diagrams at build time
npm run start                     # develop at http://localhost:3000
npm run build                     # static site in build/
```

Authoring conventions live in [AGENTS.md](./AGENTS.md) (for AI agents and
humans alike) and on the [Authoring](./docs/authoring.md) page.
