#!/usr/bin/env node
/** create-pokedocs CLI entry point — scaffolding lands with M1 (S1.1.1). */

console.error(
  'create-pokedocs is not implemented yet — follow along at github.com/wbaxterh/pokedocs',
);
process.exit(1);
